#include<stdio.h>
int main()
{
int a[100],i,n,sum=0;
printf("enter limit");
scanf("%d",&n);
for(i=0;i<n;i++)
{
printf("enter n numbers");
scanf("%d",&a[i]);
}
for(i=0;i<n;i++)
{
sum=sum+a[i];
}
printf("sum of array=%d",sum);
}
